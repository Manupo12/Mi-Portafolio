---

// Componente simple que muestra un título de sección
// precedido por un símbolo '#' en color esmeralda.
// Props:
//   - titulo (string): el texto que se mostrará como título de la sección.
// Nota: No se modifica la lógica; solo se añaden comentarios para facilitar
// la comprensión del componente.
const { titulo } = Astro.props;
---
<!--
  Estructura HTML:
  - <h3>: título principal de la sección con clases responsivas y de estilo.
  - <span>: muestra el '#', con color esmeralda para acento visual.
  - {titulo}: prop que inyecta el texto del título.
-->
<h3 class="text-2xl md:text-3xl font-bold text-white tracking-tight">
  <span class="text-emerald-400">#</span> {titulo}
</h3>